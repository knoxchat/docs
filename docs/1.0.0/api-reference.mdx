---
id: api-reference
title: "API Reference"
description: "This is a sample server Petstore server - Users API only."
sidebar_label: API Reference
sidebar_position: 0
hide_title: true
custom_edit_url: null
---
import ApiLogo from "@theme/ApiLogo";
import Heading from "@theme/Heading";
import SchemaTabs from "@theme/SchemaTabs";
import TabItem from "@theme/TabItem";
import Export from "@theme/ApiExplorer/Export";

# API Reference

Knox's request and response pattern is very similar to the OpenAI Chat API, with only minor differences. At a high level, **Knox standardizes the schema across models and providers**, so you only need to learn one.

## Base URL

All API requests should be sent to the following base URL:

```
https://api.knox.chat/v1
```

## Authentication

Each API request requires your API key to be included in the request header:

```
Authorization: Bearer sk-...
```


You can find or create API keys on the [**Knox Token**](https://knox.chat/keys) page.

## Main Endpoints

The Knox API includes the following main endpoints:

### Text Generation

- [**Text Completion**](/1.0.0/completion) - Send a text completion request to the selected model
- [**Chat Completion**](/1.0.0/chat-completion) - Send a chat completion request to the selected model

### Model Information

- [**Get List of Available Models**](/1.0.0/list-available-models) - Get information about all available models
- [**List All Video Generation Models**](/1.0.0/list-video-models) - Get the catalog of video generation models and their supported options
- [**List Music Models**](/1.0.0/list-music-models) - Get music model IDs, duration limits, and per-minute pricing
- [**List Text-to-Speech Models**](/1.0.0/list-tts-models) - Get TTS model IDs, character limits, and per-1K pricing
- [**List Speech-to-Text Models**](/1.0.0/list-stt-models) - Get STT model IDs and per-hour pricing
- [**List Voice Changer Models**](/1.0.0/list-sts-models) - Get speech-to-speech model IDs and per-minute pricing
- [**List Voice Isolator Models**](/1.0.0/list-isolation-models) - Get isolator model IDs and per-minute pricing
- [**List Sound Effects Models**](/1.0.0/list-sfx-models) - Get sound-effects model IDs and per-minute pricing
- [**List Dubbing Models**](/1.0.0/list-dubbing-models) - Get dubbing model IDs and per-minute pricing
- [**List Forced Alignment Models**](/1.0.0/list-alignment-models) - Get alignment model IDs and per-hour pricing

### Realtime

Speech-to-speech voice agents over WebSocket (`gpt-realtime-2.1`, `gpt-realtime-2.1-mini`). This is not chat completions. See the [**Realtime**](/realtime) guide for session config, billing, and hosted web search.

- [**Realtime Session**](/1.0.0/realtime) - WebSocket conversation session (`GET` / `POST` `/v1/realtime`)
- [**Create Realtime Client Secret**](/1.0.0/realtime-client-secrets) - Mint a Knox ephemeral key (`ek_knox_…`, ~60s)
- [**Create Realtime Call**](/1.0.0/realtime-calls) - WebRTC SDP exchange (`POST /v1/realtime/calls`)

### Video Generation

Video generation is asynchronous: submit a job, poll until it completes, then download the file. See the [**Video Generation**](/video-generation) guide for a full walkthrough.

- [**Submit a Video Generation Request**](/1.0.0/create-video) - Create an async video job (`POST /v1/videos`, returns `202`)
- [**Poll Video Generation Status**](/1.0.0/get-video) - Poll `GET /v1/videos/{jobId}` until `completed`
- [**Download Generated Video Content**](/1.0.0/get-video-content) - Download raw video bytes (`GET /v1/videos/{jobId}/content`)
- [**List All Video Generation Models**](/1.0.0/list-video-models) - List models, resolutions, durations, and pricing SKUs

### Music

Compose original songs from a prompt or composition plan. These routes are documented for [KnoxStudio](https://knoxstudio.ai) at `https://api.knoxstudio.ai/v1` (same backend as `https://api.knox.chat/v1`). See the [**Music**](/music) guide.

- [**List Music Models**](/1.0.0/list-music-models) - Catalog (`GET /v1/music/models`)
- [**Compose Music**](/1.0.0/compose-music) - Generate a song (`POST /v1/music`)
- [**Stream Music**](/1.0.0/stream-music) - Stream audio (`POST /v1/music/stream`)
- [**Compose Music with Details**](/1.0.0/compose-detailed-music) - Audio plus metadata (`POST /v1/music/detailed`)
- [**Stream Music with Details**](/1.0.0/compose-detailed-stream-music) - SSE audio and metadata (`POST /v1/music/detailed/stream`)
- [**Create Composition Plan**](/1.0.0/create-composition-plan) - Plan a song without generating audio (`POST /v1/music/plan`)
- [**Upload Music**](/1.0.0/upload-music) - Upload a track for inpainting (`POST /v1/music/upload`)
- [**Separate Stems**](/1.0.0/separate-stems) - Split a mix into stems (`POST /v1/music/stem-separation`)
- [**Video to Music**](/1.0.0/video-to-music) - Generate a soundtrack from video (`POST /v1/music/video-to-music`)
- [**List / Create / Get / Update / Delete Finetunes**](/1.0.0/list-music-finetunes) - Manage music finetunes (`/v1/music/finetunes`)

### Text to Speech

Convert text to audio with ElevenLabs voices. Base URL: `https://api.knoxstudio.ai/v1`. Authenticate with `Authorization: Bearer sk-...` or `xi-api-key`. See the [**Text to Speech**](/text-to-speech) guide.

- [**List Text-to-Speech Models**](/1.0.0/list-tts-models) - Catalog (`GET /v1/text-to-speech/models`)
- [**Create Speech**](/1.0.0/convert-speech) - Full audio file (`POST /v1/text-to-speech/{voice_id}`)
- [**Stream Speech**](/1.0.0/stream-speech) - Audio stream (`POST /v1/text-to-speech/{voice_id}/stream`)
- [**Create Speech with Timing**](/1.0.0/convert-speech-with-timestamps) - Audio plus character timestamps
- [**Stream Speech with Timing**](/1.0.0/stream-speech-with-timestamps) - Streaming audio plus timestamps
- [**Text-to-Speech WebSocket**](/1.0.0/tts-stream-input) - Chunked text in, audio out (`/v1/text-to-speech/{voice_id}/stream-input`)
- [**Multi-context TTS WebSocket**](/1.0.0/tts-multi-stream-input) - Multiple concurrent contexts

### Speech to Text

Transcribe audio or video with Scribe. Base URL: `https://api.knoxstudio.ai/v1`. See the [**Speech to Text**](/speech-to-text) guide.

- [**List Speech-to-Text Models**](/1.0.0/list-stt-models) - Catalog (`GET /v1/speech-to-text/models`)
- [**Create Transcript**](/1.0.0/convert-speech-to-text) - File or URL transcription (`POST /v1/speech-to-text`)
- [**Get Transcript**](/1.0.0/get-transcript) - Retrieve a stored transcript
- [**Delete Transcript**](/1.0.0/delete-transcript) - Delete a stored transcript
- [**Realtime Speech-to-Text**](/1.0.0/stt-realtime) - WebSocket streaming transcription (`/v1/speech-to-text/realtime`)

### Voices

Search, clone, and manage ElevenLabs voices. Base URL: `https://api.knoxstudio.ai/v1` (`GET /v2/voices` for search). **Not billed** as audio minutes. See the [**Voices**](/voices) guide.

- [**List Voices**](/1.0.0/list-voices) - Workspace voices (`GET /v1/voices`)
- [**Search Voices**](/1.0.0/search-voices) - Filtered search (`GET /v2/voices`)
- [**Get / Edit / Delete Voice**](/1.0.0/get-voice) - Manage a voice
- [**Create Instant Voice Clone**](/1.0.0/create-ivc-voice) - IVC from samples (`POST /v1/voices/add`)
- [**Shared Voice Library**](/1.0.0/list-shared-voices) - Browse and add shared voices
- [**Voice Settings**](/1.0.0/get-voice-settings) - Get/edit settings and defaults
- [**PVC**](/1.0.0/create-pvc-voice) - Professional Voice Clone create, samples, train, verify

### Voice Changer

Restyle source audio with a target `voice_id`. Base URL: `https://api.knoxstudio.ai/v1`. Billed at **`$0.17`/minute**. See the [**Voice Changer**](/voice-changer) guide.

- [**List Voice Changer Models**](/1.0.0/list-sts-models) - Catalog (`GET /v1/speech-to-speech/models`)
- [**Convert Speech to Speech**](/1.0.0/convert-speech-to-speech) - Full audio file (`POST /v1/speech-to-speech/{voice_id}`)
- [**Stream Speech to Speech**](/1.0.0/stream-speech-to-speech) - Audio stream (`POST /v1/speech-to-speech/{voice_id}/stream`)

### Voice Isolator

Isolate speech from mixed audio or video. Base URL: `https://api.knoxstudio.ai/v1`. Billed at **`$0.17`/minute**. See the [**Voice Isolator**](/voice-isolator) guide.

- [**List Voice Isolator Models**](/1.0.0/list-isolation-models) - Catalog (`GET /v1/audio-isolation/models`)
- [**Isolate Audio**](/1.0.0/isolate-audio) - Full audio file (`POST /v1/audio-isolation`)
- [**Stream Isolated Audio**](/1.0.0/stream-isolate-audio) - Audio stream (`POST /v1/audio-isolation/stream`)
- [**List / Delete History**](/1.0.0/list-isolation-history) - Isolation history (`/v1/audio-isolation/history`)

### Sound Effects

Generate short royalty-free clips from text. Base URL: `https://api.knoxstudio.ai/v1`. Billed at **`$0.17`/minute**. See the [**Sound Effects**](/sound-effects) guide.

- [**List Sound Effects Models**](/1.0.0/list-sfx-models) - Catalog (`GET /v1/sound-generation/models`)
- [**Create Sound Effect**](/1.0.0/convert-sound-effect) - Generate audio (`POST /v1/sound-generation`)

### Dubbing

Dub source audio or video into another language. Base URL: `https://api.knoxstudio.ai/v1`. First language is billed on project create (`$2.25`/min `dubbing_v2`, `$0.38`/min `dubbing_v1`). See the [**Dubbing**](/dubbing) guide.

- [**List Dubbing Models**](/1.0.0/list-dubbing-models) - Catalog (`GET /v1/dubbing/models`)
- [**Create / List / Get / Delete Project**](/1.0.0/create-dubbing-project) - Projects (`/v1/dubbing/project`)
- [**Language Targets**](/1.0.0/create-dubbing-language) - Add, list, get, delete languages
- [**Source Transcript**](/1.0.0/get-dubbing-source-transcript) - Get and edit source segments
- [**Target Transcript**](/1.0.0/get-dubbing-target-transcript) - Get, edit, and [regenerate](/1.0.0/regenerate-dubbing-target)

### Forced Alignment

Align spoken audio to a transcript. Base URL: `https://api.knoxstudio.ai/v1`. Billed at **`$0.27`/hour**. See the [**Forced Alignment**](/forced-alignment) guide.

- [**List Forced Alignment Models**](/1.0.0/list-alignment-models) - Catalog (`GET /v1/forced-alignment/models`)
- [**Create Forced Alignment**](/1.0.0/create-forced-alignment) - Character- and word-level timestamps (`POST /v1/forced-alignment`)

## Parameters

Knox supports various parameters to control model behavior and output. See the [**Parameters**](/1.0.0/parameters) section for details.

## Error Handling

The API uses standard HTTP status codes to indicate the result of a request:

- `200` - Request successful
- `202` - Request accepted (asynchronous video generation)
- `400` - Invalid or missing request parameters (including HTTP to a WebSocket endpoint without an upgrade)
- `401` - Authentication failed (invalid or expired API key)
- `402` - Insufficient account balance
- `403` - Token is not authorized to use the requested model
- `404` - Requested resource not found
- `422` - Validation error (music, text-to-speech, speech-to-text, voices, voice changer, isolator, sound effects, dubbing, forced alignment)
- `429` - Rate limit exceeded
- `500` - Internal server error
- `502` - Upstream Realtime provider unavailable or connect failed

Video generation endpoints return errors as `{ "error": { "code": 400, "message": "..." } }`. A poll response with `status: "failed"` is still HTTP `200`.

Music, text-to-speech, speech-to-text, voices, voice changer, Voice Isolator, sound effects, dubbing, and forced alignment endpoints return ElevenLabs-style errors: `{ "detail": "..." }` or `{ "detail": [{ "loc": [...], "msg": "...", "type": "value_error" }] }` for `422`. These routes also accept `xi-api-key` in addition to `Authorization: Bearer`.

## Requests

### Output Generation Request Format

Below is the TypeScript type definition of the request schema. This will serve as the request body when you send a `POST` request to the `/v1/chat/completions` endpoint (see [Quick Start](/intro) above for an example).

For a complete list of parameters, see the [Parameters](/1.0.0/parameters) section.

  ```typescript showNumberLine 请求模式
  // Definitions of subtypes are below
  type Request = {
    // Either "messages" or "prompt" is required
    messages?: Message[];
    prompt?: string;

    // If "model" is unspecified, uses the user's default
    model?: string; // See "Supported Models" section

    // Allows to force the model to produce specific output format.
    // See models page and note on this docs page for which models support it.
    response_format?: { type: 'json_object' };

    stop?: string | string[];
    stream?: boolean; // Enable streaming

    // See LLM Parameters (docs.knox.chat/api-reference/parameters)
    max_tokens?: number; // Range: [1, context_length)
    temperature?: number; // Range: [0, 2]

    // Tool calling
    // Will be passed down as-is for providers implementing OpenAI's interface.
    // For providers with custom interfaces, we transform and map the properties.
    // Otherwise, we transform the tools into a YAML template. The model responds with an assistant message.
    tools?: Tool[];
    tool_choice?: ToolChoice;

    // Advanced optional parameters
    seed?: number; // Integer only
    top_p?: number; // Range: (0, 1]
    top_k?: number; // Range: [1, Infinity) Not available for OpenAI models
    frequency_penalty?: number; // Range: [-2, 2]
    presence_penalty?: number; // Range: [-2, 2]
    repetition_penalty?: number; // Range: (0, 2]
    logit_bias?: { [key: number]: number };
    top_logprobs: number; // Integer only
    min_p?: number; // Range: [0, 1]
    top_a?: number; // Range: [0, 1]

    // Reduce latency by providing the model with a predicted output
    // https://platform.openai.com/docs/guides/latency-optimization#use-predicted-outputs
    prediction?: { type: 'content'; content: string };

    // Knox-only parameters
    // See "Prompt Transforms" section: docs.knox.chat/message-transforms
    transforms?: string[];
    // See "Model Routing" section: docs.knox.chat/model-routing
    models?: string[];
    route?: 'fallback';
    // See "Provider Routing" section: docs.knox.chat/provider-routing
    provider?: ProviderPreferences;
  };

  // Subtypes:

  type TextContent = {
    type: 'text';
    text: string;
  };

  type ImageContentPart = {
    type: 'image_url';
    image_url: {
      url: string; // URL or base64 encoded image data
      detail?: string; // Optional, defaults to "auto"
    };
  };

  type ContentPart = TextContent | ImageContentPart;

  type Message =
    | {
        role: 'user' | 'assistant' | 'system';
        // ContentParts are only for the "user" role:
        content: string | ContentPart[];
        // If "name" is included, it will be prepended like this
        // for non-OpenAI models: `{name}: {content}`
        name?: string;
      }
    | {
        role: 'tool';
        content: string;
        tool_call_id: string;
        name?: string;
      };

  type FunctionDescription = {
    description?: string;
    name: string;
    parameters: object; // JSON Schema object
  };

  type Tool = {
    type: 'function';
    function: FunctionDescription;
  };

  type ToolChoice =
    | 'none'
    | 'auto'
    | {
        type: 'function';
        function: {
          name: string;
        };
      };
  ```
The `response_format` parameter ensures that you receive structured responses from the large language model (LLM). This parameter is only supported by OpenAI models, Nitro models, and some other partial models.

:::info Non-standard parameter
  If the selected model does not support a request parameter (for example, `logit_bias` in non-OpenAI models, or `top_k` in OpenAI), the parameter will be ignored.
  The remaining parameters will be forwarded to the underlying model API.
:::

### Prefilled Assistant

Knox supports having the model complete partial responses. This can be used to guide the model to answer in a specific way.

To use this feature, simply include a message with `role: "assistant"` at the end of the messages array.

  ```typescript title="TypeScript"
  fetch('https://api.knox.chat/v1/chat/completions', {
    method: 'POST',
    headers: {
      Authorization: 'Bearer <KNOXCHAT_API_KEY>',
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      model: 'openai/gpt-5.2',
      messages: [
        { role: 'user', content: 'What is the meaning of life?' },
        { role: 'assistant', content: "I'm not sure, but my best guess is" },
      ],
    }),
  });
  ```

### Images and Multimodal

Multimodal image requests are implemented via the `/v1/chat/completions` API, requiring a multi-part `messages` parameter. `image_url` can be either a URL or base64-encoded image data. Live speech-to-speech uses [`/v1/realtime`](/1.0.0/realtime) instead.

```typescript
"messages": [
  {
    "role": "user",
    "content": [
      {
        "type": "text",
        "text": "What's in this image?"
      },
      {
        "type": "image_url",
        "image_url": {
          "url": "https://upload.wikimedia.org/wikipedia/commons/thumb/d/dd/Gfp-wisconsin-madison-the-nature-boardwalk.jpg/2560px-Gfp-wisconsin-madison-the-nature-boardwalk.jpg"
        }
      }
    ]
  }
]
```

LLM response example:

```json
{
  "choices": [
    {
      "role": "assistant",
      "content": "This image depicts a scenic natural landscape featuring a long wooden boardwalk that stretches out through an expansive field of green grass. The boardwalk provides a clear path and invites exploration through the lush environment. The scene is surrounded by a variety of shrubbery and trees in the background, indicating a diverse plant life in the area."
    }
  ]
}
```

### Image Generation

Some models support native image generation capabilities. To generate images, you can include `modalities: ["image", "text"]` in your request. The model will return images in OpenAI ContentPartImage format, where `image_url` contains a base64 data URL.

```typescript
{
  "model": "openai/dall-e-3",
  "messages": [
    {
      "role": "user",
      "content": "Create a beautiful sunset over mountains"
    }
  ],
  "modalities": ["image", "text"]
}
```

Image generation response example:

```json
{
  "choices": [
    {
      "message": {
        "role": "assistant",
        "content": [
          {
            "type": "text",
            "text": "Here's your requested sunset over mountains."
          },
          {
            "type": "image_url",
            "image_url": {
              "url": "data:image/png;base64,..."
            }
          }
        ]
      }
    }
  ]
}
```

#### Upload Base64 Encoded Images

For locally stored images, you can send them to the model through Base64 encoding. Here is an example:

```typescript TypeScript
import { readFile } from "fs/promises";

const getFlowerImage = async (): Promise<string> => {
  const imagePath = new URL("flower.jpg", import.meta.url);
  const imageBuffer = await readFile(imagePath);
  const base64Image = imageBuffer.toString("base64");
  return `data:image/jpeg;base64,${base64Image}`;
};

...

"messages": [
  {
    role: "user",
    content: [
      {
        type: "text",
        text: "What's in this image?",
      },
      {
        type: "image_url",
        image_url: {
          url: `${await getFlowerImage()}`,
        },
      },
    ],
  },
];
```

When sending base64 encoded data strings, ensure that the content-type of the image is included. Example:


  ```txt
  data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAgAAAAIAQMAAAD+wSzIAAAABlBMVEX///+/v7+jQ3Y5AAAADklEQVQI12P4AIX8EAgALgAD/aNpbtEAAAAASUVORK5CYII
  ```

Supported image types:

* `image/png`
* `image/jpeg`
* `image/webp`

## Response

### CompletionsResponse Format

Knox standardizes the schema across models and providers to conform to the [OpenAI Chat API](https://platform.openai.com/docs/api-reference/chat).

This means `choices` is always an array, even when the model only returns a single completion. If a streaming response was requested, each choice will contain a `delta` property; otherwise, it will contain a `message` property. This makes it easier to use the same code for all models.

The response schema in TypeScript types is as follows:

```typescript TypeScript
// Definitions of subtypes are below
type Response = {
  id: string;
  // Depending on whether you set "stream" to "true" and
  // whether you passed in "messages" or a "prompt", you
  // will get a different output shape
  choices: (NonStreamingChoice | StreamingChoice | NonChatChoice)[];
  created: number; // Unix timestamp
  model: string;
  object: 'chat.completion' | 'chat.completion.chunk';

  system_fingerprint?: string; // Only present if the provider supports it

  // Usage data is always returned for non-streaming.
  // When streaming, you will get one usage object at
  // the end accompanied by an empty choices array.
  usage?: ResponseUsage;
};
```

```typescript
// If the provider returns usage, we pass it down
// as-is. Otherwise, we count using the GPT-4 tokenizer.

type ResponseUsage = {
  /** Including images and tools if any */
  prompt_tokens: number;
  /** The tokens generated */
  completion_tokens: number;
  /** Sum of the above two fields */
  total_tokens: number;
};
```

```typescript
// Subtypes:
type NonChatChoice = {
  finish_reason: string | null;
  text: string;
  error?: ErrorResponse;
};

type NonStreamingChoice = {
  finish_reason: string | null;
  native_finish_reason: string | null;
  message: {
    content: string | null;
    role: string;
    tool_calls?: ToolCall[];
  };
  error?: ErrorResponse;
};

type StreamingChoice = {
  finish_reason: string | null;
  native_finish_reason: string | null;
  delta: {
    content: string | null;
    role?: string;
    tool_calls?: ToolCall[];
  };
  error?: ErrorResponse;
};

type ErrorResponse = {
  code: number; // See "Error Handling" section
  message: string;
  metadata?: Record<string, unknown>; // Contains additional error information such as provider details, the raw error message, etc.
};

type ToolCall = {
  id: string;
  type: 'function';
  function: FunctionCall;
};
```

Example:

```json
{
  "id": "gen-xxxxxxxxxxxxxx",
  "choices": [
    {
      "finish_reason": "stop", // Normalized finish_reason
      "native_finish_reason": "stop", // The raw finish_reason from the provider
      "message": {
        // will be "delta" if streaming
        "role": "assistant",
        "content": "Hello there!"
      }
    }
  ],
  "usage": {
    "prompt_tokens": 0,
    "completion_tokens": 4,
    "total_tokens": 4
  },
  "model": "anthropic/claude-sonnet-4.6" // Could also be "anthropic/claude-2.1", etc, depending on the "model" that ends up being used
}
```

### Finish Reasons

Knox normalizes the `finish_reason` for each model to one of the following values: `tool_calls`, `stop`, `length`, `content_filter`, or `error`.

Some models and providers may include additional completion reasons. The original `finish_reason` string returned by the model can be accessed via the `native_finish_reason` attribute.

### Query Costs and Statistics

The token counts returned in the output generation/completion API response are **not** calculated using the model's native tokenizer, but rather a standardized, model-agnostic count (implemented via the GPT-5.2 tokenizer). This is because some providers cannot reliably return native token counts. However, this behavior is becoming increasingly rare, and we may add native token counts to the response object in the future.

Balance usage and model pricing are based on **native** token counts (not the "standardized" token counts returned in the API response).
      